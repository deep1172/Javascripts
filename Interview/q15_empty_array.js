// How would you empty an array in js ?
// don't reset it to a new array and don't loop trough  pop each value 

let arr = [1,2,3,4,7,5];

console.log(arr.length);
arr.length = 0;
console.log(arr.length);

